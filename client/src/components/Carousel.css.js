import styled from "styled-components";

const Carousel = styled.div`

.recoms {
  display: flex;
  position: relative;
  flex-direction: row;
  overflow: hidden;
}

.innerRecs{
  display: flex;
  position: relative;
  flex-direction: row;
}

`;

export default Carousel;
